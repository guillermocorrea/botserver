/**
**/
process.env.NODE_ENV = 'test';

var should = require('should');
var app = require('../src/backend');

describe("Websockets Server", function() {
	it("Should broadcast a new movement event", function(done) {
		false.should.be.true("not implemented.");
	});
});